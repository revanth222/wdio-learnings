console.log("Hello, World!")

// Literals concept

/** Datatypes
 * string '' ""
 * number 
 * boolean true false
 * undefined
 * null
 * bigint
 * object type(Object, array, date)
 * symbol
 */

// string
let str= 'Revanth'
let str2 = "Routhu"
// Using literal
let str3 = `${str}_Hitler`
console.log(str3)

// number
let num = 123

// boolean
let booleanType = true

// undefined and null
let undefType
let nullType= null

// Array
let arr1 =[]

// object
let obj={}

//Regular Expression Regex
let Regex = /123/
console.log(Regex);

/**
 * typeof
 * 
 * this is an operator used to idetify the type of variables, functions
 */

// synatx
// typeof <any_JS_ componnet>
// typeof(any_JS_ componnet)

console.log(`The value of nullType is: ${nullType}`);
console.log(`The type of nullType is: ${typeof nullType}`);
console.log(`The type of string is: ${typeof(str3)}`);
console.log(`The type of number is: ${typeof(num)}`);
console.log(`The type of undefined is: ${typeof(undefType)}`);
console.log(`The type of array is: ${typeof(arr1)}`);
console.log(`The type of object is: ${typeof(obj)}`);
console.log(`The type of regular expression is: ${typeof(Regex)}`);
console.log(`The type of null is: ${typeof(nullType)}`);














